🛠 HaritoraX2 × Webカメラ ハイブリッド・トラッキング・システム設計書1. システム概要HaritoraX2（IMU方式）の「回転速度」と、Webカメラ（光学方式）の「絶対座標」を統合し、磁気乱れや布団による遮蔽に強いフルトラ環境を構築する。2. トラッキング・ロジック（ステートマシン）カメラの解析状況（信頼度スコア）に応じて、以下の3つのモードを自動で切り替えます。① 通常モード (Visible State)条件: カメラが全身を認識している（Confidence > 0.7）動作: * 位置: Webカメラの座標を100%採用。回転: Haritoraのデータで補間し、動きをヌルヌルに。補正: Haritoraの累積誤差（ドリフト）を毎フレーム、カメラ位置で上書きリセット。② 遮蔽警戒モード (Partial Occlusion)条件: 体の一部（足など）が隠れ始めた（Confidence 0.3 〜 0.7）動作: * 判定: カメラの座標が不自然に跳ねた場合、その値を破棄。重み付け: HaritoraのIMU計算による移動予測の比率を上げる。③ 布団モード (Full Occlusion / Under the Blanket)条件: カメラが関節をロストした（Confidence < 0.3）動作: * 位置固定: アバターの腰（Hips）の座標を、ロスト直前の位置でロック。回転転送: Haritoraの回転データのみを反映させる（FKモード）。地磁気ガード: 磁気乱れが検知された場合、強制的に「モード2（6軸）」の計算アルゴリズムへ移行。3. 主要アルゴリズムの疑似コードA. 磁気グネり防止（Visual Compass）磁気センサーが狂っても、カメラで見えている「正面」を優先する処理。Pythondef update_heading(imu_quat, camera_heading):
    if camera.is_visible:
        # 磁気センサーの向きではなく、カメラから見た肩のラインを正解とする
        corrected_quat = align_to_camera(imu_quat, camera_heading)
        return corrected_quat
    return imu_quat # 布団の中ではIMUを信じる
B. スムース復帰（Position Smoothing）布団から出た瞬間のアバターのワープを防ぐ。Pythondef sync_position(current_pos, target_camera_pos):
    # 0.0〜1.0の間で徐々に近づける（線形補完）
    # 毎秒、距離の約80%を埋める設定
    new_pos = lerp(current_pos, target_camera_pos, delta_time * 5.0)
    return new_pos
4. 布団内での精度維持テクニック静止時ドリフトカット: 10秒以上大きな動きがない場合、センサーの微小なノイズをゼロとして扱い、勝手に体が滑っていくのを防ぐ。寝返り補正コマンド: 特定のジェスチャー（例：両手を頭にやる）や音声入力をトリガーに、その場の向きを「正面」として座標系を再定義する。5. 推奨開発環境役割推奨ツール/ライブラリ開発言語Python 3.10+姿勢推定AIMediaPipe (BlazePose)通信Python-OSC (VRChat等への送信用)フィルタリングカルマンフィルタ (Kalman Filter)